do {
  var tarjeta = prompt("Ingrese su número de tarjeta de 16 digitos");
  if (tarjeta === '' || parseInt(tarjeta)==0 NaN || tarjeta.length !==16) {
    alert ('Tarjeta no valida');
  }
  while (parseInt (tarjeta) === NaN || tarjeta.length !== 10)
  console.log(tarjeta);

  function luhn (numeroDeTarjeta) {
    var arregloNumero
  }
  }
}
